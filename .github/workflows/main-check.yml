name: main-check

on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Setup App
        run: make setup
        
      - name: Run Tests 🧪
        run: make test
        
      - name: Check Eslint 📝
        run: make lint
